# This is a minimal Dockerfile for app deployment.
FROM registry.access.redhat.com/ubi8/dotnet-50-runtime

ARG PROJECT
ARG ASSEMBLY

COPY ./dist/apps/Release/net5.0/${PROJECT} .

CMD ["dotnet", "${ASSEMBLY}.dll"]
